# Binaries for programs and plugins.
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`.
*.test

# Output of the go coverage tool, specifically when used with LiteIDE.
*.out

# Editor files
.idea
.vscode

# Don't track vendored files, as this is a library.
vendor

# Tooling binaries.
bin/
